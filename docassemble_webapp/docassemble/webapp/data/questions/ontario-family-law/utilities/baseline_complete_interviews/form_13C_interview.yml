---
metadata:
  title: "Ontario Family Law Form 13C: Comparison of Net Family Properties"
  short title: Form 13C
  description: Interview with baseline error handling for 42 fields
  form_number: 13C
  field_count: 42
  error_handling: baseline
  generated: "2025-08-26T23:10:53.700572" 
---
include:
  - docassemble.base:data/questions/basic-questions.yml
---
features:
  debug: True
  question help button: True
  question back button: True
  progress bar: True
  progress bar method: all
  progress bar multiplier: 0.05
---
objects:
  - validation_errors: DAList
  - error_log: DAList
  - field_errors: DADict
---
initial: True
code: |
  validation_errors = DAList()
  error_log = DAList()
  field_errors = DADict()
  debug_mode = get_config('debug', False)
  error_count = 0
---
code: |
  def handle_error(context, error_type, details):
    '''Global error handler'''
    error_entry = {
      'timestamp': current_datetime(),
      'context': context,
      'type': error_type,
      'details': details
    }
    error_log.append(error_entry)
    if debug_mode:
      log(f"ERROR in {context}: {error_type} - {details}", 'error')
    return error_entry
---
code: |
  def safe_get(obj, attr, default=""):
    '''Safely get object attribute'''
    try:
      if hasattr(obj, attr):
        val = getattr(obj, attr)
        return val if val is not None else default
      return default
    except:
      return default
---
code: |
  def validate_required(field_name, value):
    '''Validate required field'''
    if not value or (isinstance(value, str) and not value.strip()):
      field_errors[field_name] = f"{field_name} is required"
      return False
    return True
---
objects:
  - user: Individual
  - spouse: Individual
  - income: DAObject
  - expenses: DAObject
  - assets: DAList.using(object_type=Individual)
  - debts: DAList.using(object_type=Individual)
---
mandatory: True
code: |
  try:
    intro_seen
  user.name.first
  spouse.name.first
  income_collected
  expenses_collected
  assets_count
  if assets_count > 0:
    assets.gather()
  debts_count
  if debts_count > 0:
    debts.gather()
  income_collected
  expenses_collected
  if has_assets:
    assets.gather()
  if has_debts:
    debts.gather()
  review_answers
  if len(validation_errors) > 0:
    review_with_errors
  form_complete
  except Exception as e:
    handle_error("main_flow", "FlowError", str(e))
    error_recovery
---
question: |
  Ontario Family Law Form 13C
subquestion: |
  Welcome to the Form 13C interview.
  
  This interview will collect 42 fields of information.
  
  % if debug_mode:
  <div class="alert alert-info">
    Debug mode is enabled - detailed error information will be shown
  </div>
  % endif
  
  All fields include validation and error handling.
continue button field: intro_seen
---
question: |
  Table 1, Row 10
fields:
  - Full legal name Full legal name Full legal name: full_legal_name_fullegal_name
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_1__row_10_collected
---
question: |
  Table 1, Row 11
fields:
  - Address Address Address: address_address
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_1__row_11_collected
---
question: |
  Table 1, Row 15
fields:
  - Full legal name Full legal name Full legal name: full_legal_name_fullegal_name
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_1__row_15_collected
---
question: |
  Table 1, Row 16
fields:
  - Address Address Address: address_address
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_1__row_16_collected
---
question: |
  Table 2, Row 1
fields:
  - Valuation Date Statement Date Statement Date Statement Date Statement Date: valuation_date_statement_date_state
    datatype: date
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_2__row_1_collected
---
question: |
  Table 2, Row 8
fields:
  - Matrimonial Home Matrimonial Home $ $ $ $ $: matrimonial_home
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_2__row_8_collected
---
question: |
  Table 2, Row 9
fields:
  - $ $ $ $ $: field
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_2__row_9_collected
---
question: |
  Table 2, Row 10
fields:
  - $ $ $ $ $: field
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_2__row_10_collected
---
question: |
  Table 2, Row 11
fields:
  - (A) TOTALS Value of Land (A) TOTALS Value of Land (A) TOTALS Value of Land (A) TOTALS Value of L: a_totals_value_of_land_a_to
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_2__row_11_collected
---
question: |
  Table 3, Row 5
fields:
  - Household goods & furniture $ $ $ $: household_goods_furniture
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_3__row_5_collected
---
question: |
  Table 3, Row 6
fields:
  - Cars, boats, vehicles $ $ $ $: cars_boats_vehicles
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_3__row_6_collected
---
question: |
  Table 3, Row 7
fields:
  - Jewellery, art, electronics, tools, sports & hobby, equipment $ $ $ $: jewellery_art_electronics_toolsports_hobby_equip
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_3__row_7_collected
---
question: |
  Table 3, Row 8
fields:
  - Other special items $ $ $ $: other_special_items
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_3__row_8_collected
---
question: |
  Table 3, Row 11
fields:
  - (B) TOTALS Value of General Household Items and Vehicles (B) TOTALS Value of General Household Ite: b_totals_value_of_general_household_items_and_vehi
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_3__row_11_collected
---
question: |
  Table 4, Row 5
fields:
  - $ $ $ $: field
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_4__row_5_collected
---
question: |
  Table 4, Row 6
fields:
  - $ $ $ $: field
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_4__row_6_collected
---
question: |
  Table 4, Row 7
fields:
  - $ $ $ $: field
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_4__row_7_collected
---
question: |
  Table 4, Row 8
fields:
  - (C) TOTALS Value of Accounts and Savings (C) TOTALS Value of Accounts and Savings (C) TOTALS Valu: c_totals_value_of_accounts_and_savings_c_totals_va
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_4__row_8_collected
---
question: |
  Table 5, Row 2
fields:
  - COMPANY TYPE & POLICY NO. OWNER BENEFICIARY FACE
AMOUNT ($) COMMENTS Document Number Applicant’s Pos: company_type_policy_nowner_beneficiary_face_amou
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_5__row_2_collected
---
question: |
  Table 5, Row 3
fields:
  - COMPANY TYPE & POLICY NO. OWNER BENEFICIARY FACE
AMOUNT ($) COMMENTS Document Number APPLICANT RESPO: company_type_policy_nowner_beneficiary_face_amou
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_5__row_3_collected
---
question: |
  Table 5, Row 8
fields:
  - (D) TOTALS Cash Surrender Value of Insurance Policies (D) TOTALS Cash Surrender Value of Insurance: d_totals_cash_surrender_value_of_insurance_policie
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_5__row_8_collected
---
question: |
  Table 6, Row 8
fields:
  - (E) TOTALS Value of Business Interests (E) TOTALS Value of Business Interests (E) TOTALS Value of: e_totals_value_of_business_interests_e_totals_valu
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_6__row_8_collected
---
question: |
  Table 7, Row 8
fields:
  - (F) TOTALS Money Owed to You (F) TOTALS Money Owed to You (F) TOTALS Money Owed to You $ $ $ $: f_totals_money_owed_to_you_f_totals_money_owed_to_
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_7__row_8_collected
---
question: |
  Table 8, Row 8
fields:
  - (G) TOTALS Value of Other Property (G) TOTALS Value of Other Property (G) TOTALS Value of Other P: g_totals_value_of_other_property_g_totals_value_of
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_8__row_8_collected
---
question: |
  Table 9, Row 1
fields:
  - VALUE OF PROPERTY OWNED ON THE VALUATION DATE, (TOTAL 1)
(Add item A to item G inclusive) $ $ $ $: value_of_property_owned_on_the_valuation_date_tota
    datatype: date
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_9__row_1_collected
---
question: |
  Table 10, Row 8
fields:
  - $ $ $ $: field
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_10__row_8_collected
---
question: |
  Table 10, Row 9
fields:
  - TOTALS Value of Debts and Other Liabilities, (TOTAL 2) TOTALS Value of Debts and Other Liabilities: totals_value_of_debts_and_other_liabilities_total_
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_10__row_9_collected
---
question: |
  Table 11, Row 6
fields:
  - Assets $ $ $ $: assets
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_11__row_6_collected
---
question: |
  Table 11, Row 8
fields:
  - TOTAL OF PROPERTY ITEMS TOTAL OF PROPERTY ITEMS TOTAL OF PROPERTY ITEMS $ $ $ $: total_of_property_items_to
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_11__row_8_collected
---
question: |
  Table 11, Row 10
fields:
  - Debts and other liabilities $ $ $ $: debts_and_other_liabilities
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_11__row_10_collected
---
question: |
  Table 11, Row 11
fields:
  - $ $ $ $: field
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_11__row_11_collected
---
question: |
  Table 11, Row 12
fields:
  - TOTAL OF DEBTS ITEMS TOTAL OF DEBTS ITEMS TOTAL OF DEBTS ITEMS $ $ $ $: total_of_debts_items_total_of
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_11__row_12_collected
---
question: |
  Table 11, Row 13
fields:
  - NET VALUE OF PROPERTY OWNED ON DATE OF MARRIAGE (NET TOTAL 3) NET VALUE OF PROPERTY OWNED ON DATE OF: net_value_of_property_owned_on_date_of_marriage_ne
    datatype: date
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_11__row_13_collected
---
question: |
  Table 12, Row 12
fields:
  - TOTALS Value of Excluded Property (TOTAL 4) TOTALS Value of Excluded Property (TOTAL 4) TOTALS Va: totals_value_of_excluded_property_total_4_totals_v
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_12__row_12_collected
---
question: |
  Table 13, Row 1
fields:
  - TOTAL 2 Debts and Other Liabilities $ $ $ $: total_2_debts_and_other_liabilities
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_13__row_1_collected
---
question: |
  Table 13, Row 2
fields:
  - TOTAL 3 Value of Property Owned on the Date of Marriage $ $ $ $: total_3_value_of_property_owned_on_the_date_of_mar
    datatype: date
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_13__row_2_collected
---
question: |
  Table 13, Row 3
fields:
  - TOTAL 4 Value of Excluded Property $ $ $ $: total_4_value_of_excluded_property
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_13__row_3_collected
---
question: |
  Table 13, Row 4
fields:
  - TOTAL 5 (TOTAL 2 + TOTAL 3 + TOTAL 4) $ $ $ $: total_5_total_2_total_3_total_4
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_13__row_4_collected
---
question: |
  Table 14, Row 1
fields:
  - TOTAL 1 Value of Property Owned on Valuation Date $ $ $ $: total_1_value_of_property_owned_on_valuation_date
    datatype: date
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_14__row_1_collected
---
question: |
  Table 14, Row 2
fields:
  - TOTAL 5 (from above) $ $ $ $: total_5_from_above
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_14__row_2_collected
---
question: |
  Table 14, Row 3
fields:
  - TOTAL 6 NET FAMILY PROPERTY (Subtract TOTAL 1 minus TOTAL 5) $ $ $ $: total_6_net_family_property_subtractotal_1_minus
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_14__row_3_collected
---
question: |
  Table 15, Row 3
fields:
  - $ $ $ $: field
    datatype: currency
    min: 0
validation code: |
  # Validate required fields
  for field in []:
    if not defined(field) or not value(field):
      validation_error(f"{field} is required")
continue button field: table_15__row_3_collected
---
question: |
  Your Information
fields:
  - First Name: user.name.first
    required: True
    validation messages:
      required: First name is required
  - Last Name: user.name.last
    required: True
    validation messages:
      required: Last name is required
  - Date of Birth: user.birthdate
    datatype: date
    required: False
---
question: |
  Spouse Information
fields:
  - First Name: spouse.name.first
    required: True
    validation messages:
      required: First name is required
  - Last Name: spouse.name.last
    required: True
    validation messages:
      required: Last name is required
  - Date of Birth: spouse.birthdate
    datatype: date
    required: False
---
question: |
  Income Information
fields:
  - Employment Income: income.employment
    datatype: currency
    min: 0
    required: True
  - Self-Employment Income: income.self_employment
    datatype: currency
    min: 0
    required: False
  - Other Income: income.other
    datatype: currency
    min: 0
    required: False
validation code: |
  income.total = (income.employment or 0) + (income.self_employment or 0) + (income.other or 0)
continue button field: income_collected
---
question: |
  Monthly Expenses
fields:
  - Housing: expenses.housing
    datatype: currency
    min: 0
    required: True
  - Food: expenses.food
    datatype: currency
    min: 0
    required: True
  - Transportation: expenses.transportation
    datatype: currency
    min: 0
    required: False
  - Other: expenses.other
    datatype: currency
    min: 0
    required: False
validation code: |
  expenses.total = (expenses.housing or 0) + (expenses.food or 0) + (expenses.transportation or 0) + (expenses.other or 0)
continue button field: expenses_collected
---
question: |
  How many assets are there?
fields:
  - Number: assets_count
    datatype: integer
    min: 0
    max: 20
---
question: |
  Asset ${i + 1} Information
fields:
  - First Name: assets[i].name.first
    required: True
  - Last Name: assets[i].name.last
    required: True
  - Date of Birth: assets[i].birthdate
    datatype: date
    required: False
---
question: |
  Add another asset?
yesno: assets.there_is_another
---
question: |
  How many debts are there?
fields:
  - Number: debts_count
    datatype: integer
    min: 0
    max: 20
---
question: |
  Debt ${i + 1} Information
fields:
  - First Name: debts[i].name.first
    required: True
  - Last Name: debts[i].name.last
    required: True
  - Date of Birth: debts[i].birthdate
    datatype: date
    required: False
---
question: |
  Add another debt?
yesno: debts.there_is_another
---
event: error_recovery
question: |
  Error Recovery
subquestion: |
  An error occurred during the interview.
  
  % if len(error_log) > 0:
  **Recent Errors:**
  % for error in error_log[-5:]:
  - ${ error['context'] }: ${ error['type'] }
  % endfor
  % endif
  
  You can try restarting or contact support.
buttons:
  - Restart: restart
  - Exit: exit
---
event: review_with_errors
question: |
  Review with Validation Issues
subquestion: |
  <div class="alert alert-warning">
    There are ${ len(validation_errors) } validation issues to review.
  </div>
  
  % for error in validation_errors[:10]:
  - ${ error }
  % endfor
  
  You can continue anyway or go back to fix these issues.
buttons:
  - Continue Anyway: continue
  - Go Back: restart
---
event: review_answers
question: |
  Review Your Answers
subquestion: |
  Please review the information you've provided.
  
  % if len(validation_errors) == 0:
  <div class="alert alert-success">
    ✓ All information validated successfully
  </div>
  % else:
  <div class="alert alert-warning">
    ⚠ ${ len(validation_errors) } validation warnings
  </div>
  % endif
  
  **Form 13C Summary**
  
  Information has been collected for all required fields.
  
  % if debug_mode:
  **Debug Info:**
  - Errors logged: ${ len(error_log) }
  - Fields with errors: ${ len(field_errors) }
  % endif
buttons:
  - Continue: continue
  - Edit Answers: restart
---
event: form_complete
question: |
  Form 13C Complete
subquestion: |
  Thank you for completing Form 13C.
  
  % if len(error_log) == 0:
  <div class="alert alert-success">
    ✓ Interview completed without errors
  </div>
  % else:
  <div class="alert alert-info">
    Interview completed with ${ len(error_log) } logged events
  </div>
  % endif
  
  **Summary:**
  - All required fields completed
  - Validation: ${ "Passed" if len(validation_errors) == 0 else str(len(validation_errors)) + " warnings" }
  
buttons:
  - Exit: exit
  - Start Over: restart